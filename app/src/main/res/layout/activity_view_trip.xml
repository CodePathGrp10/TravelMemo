<?xml version="1.0" encoding="utf-8"?>

<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- This LinearLayout represents the contents of the screen  -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            layout="@layout/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <com.astuetz.PagerSlidingTabStrip
            android:id="@+id/view_trip_tabstrip"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:textColor="@color/colorText"
            app:pstsShouldExpand="true"
            app:pstsTextAllCaps="true"
            app:pstsTabBackground="@color/primary"
            app:pstsIndicatorColor="@color/primary_light"
            app:pstsIndicatorHeight="3dp"/>

        <android.support.v4.view.ViewPager
            android:id="@+id/view_trip_viewpager"
            android:layout_below="@+id/view_trip_tabstrip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white" />

    </LinearLayout>
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fabAddPhoto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="16dp"
        android:src="@drawable/ic_take_photo"
        app:layout_anchor="@id/view_trip_viewpager"
        app:layout_anchorGravity="bottom|right|end"
        app:layout_behavior="com.grp10.codepath.travelmemo.utils.ScrollAwareFABBehavior" />

</android.support.design.widget.CoordinatorLayout>